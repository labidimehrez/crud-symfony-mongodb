{% for product in products %}
    </br>
    <tr> 
        id: <td> {{ product.id }}</td>
        name: <td>{{product.name}}</td>
        price:  <td>{{product.price}}</td>
    </tr>
    </br>
{% endfor %} 